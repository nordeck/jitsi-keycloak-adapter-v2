# Releasing

- Apply your changes.
- Test these changes on a test environment.
- Update `LABEL version` in Dockerfile.
- Run the action manually on GitHub to generate the Docker image.
  `Actions` -> `jitsi-keycloak-adapter-v2` -> `Run workflow`
- Create a release on GitHub. Use the same version as tag.
